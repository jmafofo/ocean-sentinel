/**
 * AdBanner — placeholder for Google AdMob banner ads.
 *
 * AdMob requires a custom Expo dev client (expo-ads-admob is deprecated;
 * react-native-google-mobile-ads needs a bare/custom build).
 * The subscription gating is fully wired — this component will show real
 * ads once AdMob is integrated in a future build.
 *
 * To enable:
 *   1. Set up an Expo custom dev client or eject to bare workflow
 *   2. Install react-native-google-mobile-ads and add plugin to app.json
 *   3. Replace this stub with the real BannerAd implementation
 */

export default function AdBanner() {
  // Stub — renders nothing until AdMob is integrated
  return null;
}
